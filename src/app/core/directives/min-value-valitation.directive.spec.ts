import { MinValueValitationDirective } from './min-value-valitation.directive';

describe('MinValueValitationDirective', () => {
  it('should create an instance', () => {
    const directive = new MinValueValitationDirective();
    expect(directive).toBeTruthy();
  });
});
