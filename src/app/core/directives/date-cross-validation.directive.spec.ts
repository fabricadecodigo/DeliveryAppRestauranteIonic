import { DateCrossValidationDirective } from './date-cross-validation.directive';

describe('DateCrossValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new DateCrossValidationDirective();
    expect(directive).toBeTruthy();
  });
});
